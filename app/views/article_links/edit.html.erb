<p>Editing the Article Link: <strong><%= @article_link.title %></strong></p>
<%= form_for(@article_link) do |f| %>
    <% if @errors %>
  <div id="error_explanation">
    <h2><%= pluralize(@errors.count, "error") %> prohibited this Text Post from being saved:</h2>

    <ul>
      <% @errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "save", class:'edit-button material-icons' %>
  </div>
  <br><br>
<% end %>